{
  "name": "octane-dev",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "serve",
    "build": "esbuild index.ts --bundle --outfile=dist/index.js --watch",
    "build-file": "node builder.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "build": "esbuild index.ts --bundle --outfile=dist/index.js --watch",
    "esbuild": "^0.20.0",
    "serve": "^14.2.1"

  },
  "dependencies": {
    "gsap": "^3.12.5",
    "swiper": "^11.0.5"
  }
}
